<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="shift_jis">
  <title>四則逆算 - 開始画面</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: sans-serif;
      background-color: #f0f8ff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      color: #007aff;
      margin-bottom: 1rem;
    }

    .start-button {
      background: url('../images/button/start.gif') no-repeat;
      width: 333px;
      height: 132px;
      border: none;
      cursor: pointer;
    }

    .start-button:hover {
      background-image: url('../images/button/start2.gif');
    }
  </style>

  <script>
    function startWebTest() {
      // 设置初始时间与问题列表
      const nowTime = new Date().getTime();
      const startTime = nowTime;
      const rlist = "001.002"; // 第1,2题
      const alist = "";

      const url = `./1.html?nowTime=${nowTime}&startTime=${startTime}&rlist=${rlist}&alist=${alist}`;
      window.location.href = url;
    }
  </script>
</head>
<body>
  <h1>四則逆算</h1>
  <button class="start-button" onclick="startWebTest()"></button>
</body>
</html>
